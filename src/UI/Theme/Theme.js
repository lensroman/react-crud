import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#242429',
    },
    secondary: {
      main: '#5797d8',
    },
    text: {
      main: '#ffffff',
    },
  },
})

export default theme;
