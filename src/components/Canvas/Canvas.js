import React from "react";
import Markup from "../../containers/Markup/Markup";

const Canvas = props => {

    return (
        <div>
            <Markup>

            </Markup>
        </div>
    );
};

export default Canvas;